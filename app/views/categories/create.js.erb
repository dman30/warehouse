<% if @category.new_record? %>
  $('input#category_name').effect('highlight', { color: 'red' });
<% else %>
  $('div#categories').append("<%= escape_javascript(render @category) %>");
  $('div#<%= dom_id(@category) %>').effect("highlight");
<% end %>
