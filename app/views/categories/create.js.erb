<% if @category.new_record? %>
  $('input#category_name').effect('highlight', { color: 'red' });
<% else %>
  $('table#categories').append("<%= escape_javascript(render @category) %>");
  $('tr#<%= dom_id(@category) %>').effect("highlight");
  $('form#new_category').get(0).reset();
<% end %>
